{
  "name": "dictator_backend",
  "version": "1.0.0",
  "description": "Reference webhook receiver for the Dictator app",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "WEBHOOK_API_KEY=test-key DATA_DIR=/tmp/dictator-backend-test node --import tsx --test 'src/**/*.test.ts'",
    "test:coverage": "WEBHOOK_API_KEY=test-key DATA_DIR=/tmp/dictator-backend-test node --import tsx --experimental-test-coverage --test 'src/**/*.test.ts'"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "fastify": "^5.12.3",
    "pino": "^9.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
